# Storage Package

Local-first storage client with:
- IndexedDB backend (extension runtime)
- memory backend fallback (CLI smoke tests)
- migration registry
- repositories for `automations`, `tableData`, `tableRows`
