# Control API baseline env
NODE_ENV=production
PORT=3000
APP_VERSION=0.1.0

# Enable DB readiness gate once DATABASE_URL reference is wired.
REQUIRE_DB=false

# Railway variable reference target:
# DATABASE_URL=${{Postgres.DATABASE_URL}}
DATABASE_URL=

