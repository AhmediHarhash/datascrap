# Control API baseline env
NODE_ENV=production
PORT=3000
APP_VERSION=0.1.0

# Keep true once migrations are applied in the target environment.
REQUIRE_DB=true

# Railway variable reference target:
# DATABASE_URL=${{Postgres.DATABASE_URL}}
DATABASE_URL=

# Rotate with high entropy random values in each environment.
JWT_ACCESS_SECRET=
JWT_ISSUER=datascrap-control-api
ACCESS_TOKEN_TTL_SECONDS=900
REFRESH_TOKEN_TTL_DAYS=30
DEFAULT_MAX_DEVICES=2

# CORS hardening
# Keep strict in production and explicitly list dashboard origins.
CORS_STRICT=true
CORS_ALLOW_NO_ORIGIN=true
CORS_ALLOWED_ORIGINS=
CORS_ALLOWED_ORIGIN_PREFIXES=chrome-extension://

# Rate limits
RATE_LIMIT_WINDOW_SECONDS=60
RATE_LIMIT_AUTH_REGISTER_MAX=10
RATE_LIMIT_AUTH_LOGIN_MAX=15
RATE_LIMIT_AUTH_REFRESH_MAX=30
RATE_LIMIT_AUTH_LOGOUT_MAX=60
RATE_LIMIT_LICENSE_REGISTER_MAX=30
RATE_LIMIT_LICENSE_STATUS_MAX=60
RATE_LIMIT_DEVICES_VALIDATE_MAX=120
RATE_LIMIT_DEVICES_MUTATE_MAX=60
RATE_LIMIT_DEVICES_LIST_MAX=120

# Idempotency replay TTL
IDEMPOTENCY_TTL_HOURS=24
